(function () {
    "use strict";

    angular
        .module('eat-to-bite')
        .controller('homeCtrl',['$scope', function ($scope) {

            $scope.works = 'Angular is here';

        }]);

})();