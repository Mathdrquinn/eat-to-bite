(function () {
    'use strict';

    angular
        .module('profile')
        .factory('profileSvc', ['$http', '$rootScope', function ($http, $cookieStore, $location, $rootScope) {


        }]);
})();